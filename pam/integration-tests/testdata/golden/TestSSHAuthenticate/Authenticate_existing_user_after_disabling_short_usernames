== Provider selection ==
  1. local
  2. ExampleBroker
(user-integration-shortusername@localhost) Choose your provider:
> 2
== Password authentication ==
Enter 'r' to cancel the request and go back to select the authentication flow
(user-integration-shortusername@localhost) Gimme your password:
>
PAM Authenticate() finished for user 'user-integration-shortusername'
 SSHD: Connected to ssh via authd module! [TestSSHAuthenticate/Authenticate_existing_user_after_disabling_short_usernames]
  HOME=${AUTHD_TEST_HOME}
  LOGNAME=user-integration-shortusername
  PATH=${AUTHD_TEST_PATH}
  PWD=${AUTHD_TEST_PWD}
  SHELL=/bin/sh
  SSH_CLIENT=${AUTHD_TEST_SSH_CLIENT}
  SSH_CONNECTION=${AUTHD_TEST_SSH_CONNECTION}
  SSH_TTY=${AUTHD_TEST_SSH_TTY}
  TERM=xterm-256color
  USER=user-integration-shortusername
Connection to localhost closed.

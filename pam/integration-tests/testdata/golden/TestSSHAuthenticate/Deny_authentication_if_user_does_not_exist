> ssh ${AUTHD_PAM_SSH_USER}@localhost ${AUTHD_PAM_SSH_ARGS}
PAM Authenticate() finished for user 'user-unexistent@example.com'
SSH PAM user 'user-unexistent@example.com' using local broker
(user-unexistent@example.com@localhost) Password:
────────────────────────────────────────────────────────────────────────────────
> ssh ${AUTHD_PAM_SSH_USER}@localhost ${AUTHD_PAM_SSH_ARGS}
PAM Authenticate() finished for user 'user-unexistent@example.com'
SSH PAM user 'user-unexistent@example.com' using local broker
(user-unexistent@example.com@localhost) Password:
────────────────────────────────────────────────────────────────────────────────
